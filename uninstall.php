<?php

/**
 * Uninstall Smart Section TOC
 *
 * @package SmartSectionTOC
 */

// Exit if accessed directly or not in uninstall context
if (! defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

// Currently, this plugin doesn't store any options or custom data
// If you add options in future versions, clean them up here:
// delete_option( 'smart_section_toc_options' );
